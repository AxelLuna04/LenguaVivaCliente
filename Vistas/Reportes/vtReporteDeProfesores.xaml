<Page x:Class="LenguaVivaCliente.Vistas.Reportes.vtReporteDeProfesores"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LenguaVivaCliente.Vistas.Reportes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Background="#FFF5F5F5"
      Title="Reporte de Profesores">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <Border Grid.Row="0" Background="#FF8E44AD" CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <TextBlock Text="REPORTE DE PROFESORES" 
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       FontFamily="Segoe UI"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"/>
        </Border>

        <!-- Tabla de profesores -->
        <Border Grid.Row="1" BorderBrush="#FFBDBDBD" BorderThickness="1" CornerRadius="5" Background="White">
            <DataGrid x:Name="dgProfesores"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          HeadersVisibility="Column"
          GridLinesVisibility="None"
          RowBackground="White"
          AlternatingRowBackground="#FFF7F7F7"
          HorizontalScrollBarVisibility="Auto"
          VerticalScrollBarVisibility="Auto"
          Margin="0">
                <DataGrid.Columns>
                    <!-- Nombre del profesor -->
                    <DataGridTextColumn Header="Profesor" 
                            Binding="{Binding NombreCompleto}" 
                            Width="*" 
                            FontSize="14" />
                    <!-- Número de cursos activos -->
                    <DataGridTextColumn Header="Cursos Activos" 
                            Binding="{Binding CursosActivos}" 
                            Width="150" 
                            FontSize="14" />
                </DataGrid.Columns>
            </DataGrid>

        </Border>

        <!-- Botón Volver -->
        <Button Grid.Row="2"
                Content="⬅️ Volver"
                HorizontalAlignment="Left"
                Width="120"
                Height="40"
                Margin="0,20,0,0"
                FontSize="14"
                FontWeight="Bold"
                Foreground="White"
                Click="Click_Volver">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#FF8E44AD"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF9B59B6"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

    </Grid>
</Page>
